# Environment variables for production builds.
# VITE_* vars are baked into the JS bundle at build time.
# Leave these unset to use the self-hosted runtime injection mechanism
# (start.sh replaces placeholder strings with actual values at container startup).
